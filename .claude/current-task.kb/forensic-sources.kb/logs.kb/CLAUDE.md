# logs.kb — maintenance guide

Sources capturing **logs and crash records** — logcat buffers, DropBox crash/ANR
history, native tombstones, and the full bugreport bundle. One file per capture;
see `../CLAUDE.md` for the shared schema and conventions.

Belongs here: a log or crash artifact. Not here: usage/process activity
(→ `../activity.kb/`).

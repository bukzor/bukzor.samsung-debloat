# telephony.kb — maintenance guide

Sources for **on-device security posture and telephony/messaging** — Play
Protect state and SMS/MMS/call records (the card-fraud thread). One file per
capture; see `../CLAUDE.md` for the shared schema and conventions.

Belongs here: verify-apps state and message/call content sources. **Message and
call content is personal data** — keep it in `forensics/` only and never quote
it in the kb; reference filenames and aggregate observations.

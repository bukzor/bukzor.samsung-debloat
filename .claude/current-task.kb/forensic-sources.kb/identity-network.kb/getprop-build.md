---
capture-status: captured
captured-as: [getprop.txt]
needs-device: false
last-updated: "2026-05-27"
---

# getprop-build — device fingerprint and patch level

`getprop`: build fingerprint, model, One UI version, and security-patch date —
device identity and a timeline anchor (e.g. the current patch level bounds when
the OS was last updated).

Capture: `adb shell getprop > getprop.txt`

This device: SM-S926U, Android 16 / One UI 8, patch 2026-04-05 (see `../../device.md`).
